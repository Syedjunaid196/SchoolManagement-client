export enum Gender {
    Male = 1,
    Female = 2,
    Other = 3,
}

export enum UserRole {
    Admin = 1,
    Teacher = 2,
    Student = 3,
    Parent = 4
}

export enum UserStatus {
    Active = 1,
    Inactive = 2,
    Suspended = 3,
    Deleted = 4
}